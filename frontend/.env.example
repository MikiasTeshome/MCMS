# API Server Base URL (production builds only; dev uses same-origin /api/v1 proxy)
# VITE_API_URL="https://your-api.example.com/api/v1"

# Default localization language (en, am)
VITE_DEFAULT_LANGUAGE="en"
